<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
			 Visual="Material"
             x:Class="ExposureNotification.App.MainPage">

	<StackLayout Margin="20, 20, 20, 20">

		<Button Text="{Binding EnableDisableText}" Command="{Binding EnableDisableCommand}" Style="{DynamicResource button}" />

		<BoxView HeightRequest="2" BackgroundColor="#505050" Margin="0,40,0,40" />
		
		<Entry Text="{Binding DiagnosisUid, Mode=TwoWay}" HorizontalOptions="FillAndExpand" Placeholder="Diagnosis Identifier" />
		<Button Text="Submit Diagnosis" Command="{Binding SubmitDiagnosisCommand}" Style="{DynamicResource button}" IsEnabled="{Binding IsEnabled}" />
	</StackLayout>

</ContentPage>
